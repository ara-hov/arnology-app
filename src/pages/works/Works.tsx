import "./works.scss";
import WorksContainer from "../../components/works-container/worksContainer";

const Works = () => {
  return (
    <div>
      <WorksContainer />
    </div>
  );
};

export default Works;
